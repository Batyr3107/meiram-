# 📱 Инструкция по Настройке и Установке APK

## ВАЖНО: Настройка API перед использованием APK

APK уже собран и находится здесь:
```
build/app/outputs/flutter-apk/app-release.apk
```

**НО! Перед установкой на телефон нужно настроить API сервер!**

---

## 🔧 Шаг 1: Настройка API сервера

### Вариант A: Сервер на вашем компьютере (локальная сеть)

1. **Узнайте IP адрес вашего компьютера:**

   **На Windows:**
   ```bash
   ipconfig
   ```

   Найдите строку "IPv4 Address", например:
   ```
   IPv4 Address. . . . . . . . . . . : 192.168.1.100
   ```

2. **Откройте файл с настройками API:**
   ```
   lib/core/constants/app_constants.dart
   ```

3. **Измените строку 8:**

   **Было:**
   ```dart
   static const String apiBaseUrl = 'http://192.168.1.100:8080';
   ```

   **Должно быть (с ВАШИМ IP):**
   ```dart
   static const String apiBaseUrl = 'http://192.168.ВАШ.IP:8080';
   ```

   Например:
   ```dart
   static const String apiBaseUrl = 'http://192.168.0.105:8080';
   ```

4. **ВАЖНО:** Убедитесь что:
   - Backend сервер запущен на вашем компьютере
   - Телефон и компьютер подключены к ОДНОЙ Wi-Fi сети
   - Порт 8080 открыт в файрволле

### Вариант B: Сервер в облаке

Если ваш backend сервер находится в облаке (AWS, Google Cloud, Heroku и т.д.):

1. **Откройте файл:**
   ```
   lib/core/constants/app_constants.dart
   ```

2. **Измените строку 8 на адрес вашего сервера:**
   ```dart
   static const String apiBaseUrl = 'https://your-domain.com';
   ```

   Или с IP:
   ```dart
   static const String apiBaseUrl = 'http://123.45.67.89:8080';
   ```

---

## 📦 Шаг 2: Пересобрать APK (после изменения API)

После изменения `app_constants.dart` нужно пересобрать APK:

```bash
# Перейти в папку проекта
cd c:\Users\JusanUser\PycharmProjects\pythonProject2\shopmobile-master\shopmobile-master

# Собрать новый APK
flutter build apk --release
```

Подождите 5-15 минут. Готовый APK будет здесь:
```
build/app/outputs/flutter-apk/app-release.apk
```

---

## 📲 Шаг 3: Установка APK на Android телефон

### Способ 1: Через USB кабель

1. **Подключите телефон к компьютеру через USB**

2. **Скопируйте APK файл на телефон:**
   - Откройте папку телефона на компьютере
   - Скопируйте файл `app-release.apk` в папку `Download` на телефоне

3. **На телефоне:**
   - Откройте приложение "Файлы" или "Downloads"
   - Найдите файл `app-release.apk`
   - Нажмите на него

4. **Разрешите установку:**
   - Если появится запрос "Установка заблокирована"
   - Нажмите "Настройки" → Включите "Разрешить из этого источника"
   - Вернитесь назад и нажмите "Установить"

### Способ 2: Через мессенджер

1. **Отправьте себе APK файл через:**
   - WhatsApp
   - Telegram
   - Email

2. **На телефоне:**
   - Откройте сообщение
   - Скачайте файл `app-release.apk`
   - Нажмите "Открыть"
   - Следуйте инструкциям установки

### Способ 3: Через ADB (для разработчиков)

```bash
# Включите "Отладку по USB" на телефоне
# Подключите телефон к компьютеру

# Установите APK
adb install build/app/outputs/flutter-apk/app-release.apk
```

---

## ⚙️ Шаг 4: Настройки безопасности на Android

### На Android 8-12:

1. **Настройки** → **Безопасность**
2. Найдите **"Неизвестные источники"**
3. Включите для приложения "Файлы" или браузера

### На Android 13+:

1. При установке появится запрос
2. Нажмите **"Настройки"**
3. Включите **"Разрешить из этого источника"**
4. Вернитесь и нажмите **"Установить"**

---

## 🚀 Шаг 5: Запуск приложения

1. **Убедитесь что backend сервер запущен**

   Проверьте что сервер работает:
   ```bash
   # Если сервер Java Spring Boot:
   java -jar backend.jar

   # Или если это другой сервер
   # Убедитесь что он запущен на порту 8080
   ```

2. **Убедитесь что телефон и сервер в одной сети**

   - Телефон подключен к Wi-Fi
   - Компьютер с сервером в той же сети
   - IP адрес в `app_constants.dart` правильный

3. **Запустите приложение на телефоне**

   - Найдите иконку "Shop Mobile"
   - Нажмите для запуска
   - Наслаждайтесь ПРЕМИУМ интерфейсом!

---

## ❓ Частые Проблемы

### Проблема 1: "Регистрация не работает"

**Причина:** Неправильный API адрес или сервер не запущен

**Решение:**
1. Проверьте IP адрес в `app_constants.dart`
2. Убедитесь что backend сервер запущен
3. Проверьте что телефон и компьютер в одной Wi-Fi сети
4. Пересоберите APK после изменения

### Проблема 2: "Не могу установить APK"

**Причина:** Запрет установки из неизвестных источников

**Решение:**
1. Настройки → Безопасность → Неизвестные источники
2. Разрешите установку для нужного приложения

### Проблема 3: "APK не открывается"

**Причина:** Файл поврежден при передаче

**Решение:**
1. Скачайте APK заново
2. Проверьте размер файла (должен быть ~22-23 MB)
3. Попробуйте другой способ передачи

### Проблема 4: "Ошибка сети при входе"

**Причина:** Телефон не может подключиться к серверу

**Решение:**
1. Проверьте Wi-Fi подключение
2. Пингуйте сервер: `ping 192.168.ВАШ.IP`
3. Проверьте файрволл на компьютере
4. Убедитесь что порт 8080 открыт

---

## 🎯 Быстрая Проверка Настройки

### Checklist перед установкой:

- [ ] Backend сервер запущен
- [ ] IP адрес компьютера известен
- [ ] Файл `app_constants.dart` изменен с правильным IP
- [ ] APK пересобран после изменения
- [ ] Телефон и компьютер в одной Wi-Fi сети
- [ ] Разрешена установка из неизвестных источников

---

## 📝 Пример Полной Настройки

```bash
# 1. Узнать IP компьютера
ipconfig
# Результат: 192.168.0.105

# 2. Изменить app_constants.dart
# Открыть lib/core/constants/app_constants.dart
# Строка 8: static const String apiBaseUrl = 'http://192.168.0.105:8080';

# 3. Собрать APK
cd c:\Users\JusanUser\PycharmProjects\pythonProject2\shopmobile-master\shopmobile-master
flutter build apk --release

# 4. APK готов!
# Путь: build/app/outputs/flutter-apk/app-release.apk

# 5. Скопировать на телефон и установить

# 6. Запустить backend сервер
# (команда зависит от вашего сервера)

# 7. Запустить приложение на телефоне
```

---

## 🎨 Что вы увидите в приложении

После успешной установки:

1. **Главный экран:**
   - Красивый gradient hero section
   - Статистика (500+ товаров, 99% довольных клиентов)
   - Список преимуществ

2. **Страница входа:**
   - Gradient иконка "С возвращением!"
   - Красивые поля ввода
   - Gradient кнопка входа

3. **Страница регистрации:**
   - Gradient иконка "Создайте аккаунт"
   - Поля для email, телефона, пароля
   - Gradient кнопка регистрации

4. **После входа:**
   - Список продавцов
   - Каталог товаров
   - Корзина
   - Профиль с переключателем темы

---

## 💡 Совет для другого ИИ

**КРАТКАЯ ВЕРСИЯ:**

1. Найди свой IP: `ipconfig`
2. Открой: `lib/core/constants/app_constants.dart`
3. Строка 8: измени `http://192.168.1.100:8080` на `http://ТВОЙ_IP:8080`
4. Собери APK: `flutter build apk --release`
5. APK здесь: `build/app/outputs/flutter-apk/app-release.apk`
6. Скопируй на телефон и установи
7. Запусти backend сервер
8. Открой приложение

**ВСЁ!** 🎉

---

## 📞 Контакты

Если возникли вопросы:
- GitHub: https://github.com/Batyr3107/meiram-
- Repository Issues: https://github.com/Batyr3107/meiram-/issues

---

**Последнее обновление:** 2025-11-18
**Версия APK:** 1.0.0+1
**Размер APK:** ~22.6 MB
